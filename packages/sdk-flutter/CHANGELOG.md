# Changelog

## 0.1.0

- Initial Flutter SDK release with checkout handoff URL generation, scanner-device check-in helpers, HMAC-verified offline manifests, offline scan persistence and sync, adapter-based camera scanner UI, ticket display, scanner status widgets, tests, and a buildable example app.
