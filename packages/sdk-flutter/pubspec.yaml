name: tixkit_flutter
description: Flutter SDK for Tixkit scanner, ticket display, checkout handoff, and offline sync.
version: 0.1.0
homepage: https://tixkit.com

environment:
  sdk: '>=3.4.0 <4.0.0'
  flutter: '>=3.22.0'

dependencies:
  crypto: ^3.0.6
  flutter:
    sdk: flutter
  http: ^1.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^4.0.0
