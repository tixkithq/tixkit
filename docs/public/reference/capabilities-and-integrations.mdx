---
title: Capabilities and integrations
description: Authoritative availability and public-boundary registry for Tixkit integrations.
audience:
  - developer
  - self-hoster
  - contributor
product_area: platform
content_type: reference
status: beta
owner: platform-integrations
last_verified: 2026-07-15
prerequisites: []
related:
  - /operators/messaging
  - /platform
---

# Capabilities and integrations

This page is generated from the public capability registry. “Available” describes an implemented public contract or adapter, not a promise that Tixkit Cloud is generally available. Managed services are shown only as private beta or planned. Cloud, Platform API, and Self-Hosted availability are independent decisions.

| ID                             | Capability                   | Classification           | Lifecycle | Availability                                                      | Extension contract                                                                                                                                                                                                 | Delivery and portability obligations                                                                                                                                                                                         | Notes                                                                                                                                                                                                                                                        |
| ------------------------------ | ---------------------------- | ------------------------ | --------- | ----------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `apple-messages-for-business`  | Apple Messages for Business  | managed-provider-service | planned   | Cloud: planned; Platform API: planned; Self-Hosted: unavailable   | Not yet versioned: A public rich-channel extension contract has not been published.                                                                                                                                | credentials=managed-cloud; plaintext-portable=false; portability=contract-metadata; rebind=required; consent=required; suppression=required; webhooks=required; retries=required; idempotency=required; audit=required       | Planned managed rich-channel service. Self-Hosted remains unavailable until a versioned public rich-channel extension contract exists.                                                                                                                       |
| `generic-messaging-contracts`  | Generic messaging contracts  | shared-core              | stable    | Cloud: available; Platform API: available; Self-Hosted: available | Versioned: `tixkit.messaging-provider/v1` via `@tixkit/domain/messaging#registerMessagingProviderExtension`; implementation: `packages/domain/src/messaging/index.ts#EmailTransport,SmsTransport`                  | credentials=caller-or-operator; plaintext-portable=false; portability=contract-metadata; rebind=required; consent=required; suppression=required; webhooks=required; retries=required; idempotency=required; audit=required  | Public transport contracts are the required boundary for provider delivery; direct database delivery paths are unsupported.                                                                                                                                  |
| `managed-email-operations`     | Managed email operations     | managed-provider-service | planned   | Cloud: planned; Platform API: planned; Self-Hosted: unavailable   | Versioned: `tixkit.messaging-provider/v1` via `@tixkit/domain/messaging#registerMessagingProviderExtension`; implementation: `packages/domain/src/messaging/index.ts#EmailTransport`                               | credentials=managed-cloud; plaintext-portable=false; portability=configuration-only; rebind=required; consent=required; suppression=required; webhooks=required; retries=required; idempotency=required; audit=required      | Planned Cloud operation may custody credentials and operate delivery, while the public transport contract remains authoritative.                                                                                                                             |
| `managed-routing-intelligence` | Managed routing intelligence | managed-intelligence     | planned   | Cloud: planned; Platform API: planned; Self-Hosted: unavailable   | Versioned: `tixkit.messaging-provider/v1` via `@tixkit/domain/messaging#registerMessagingProviderExtension`; implementation: `packages/domain/src/messaging/provider-extensions.ts#MessagingProviderRouteSelector` | credentials=managed-cloud; plaintext-portable=false; portability=contract-metadata; rebind=required; consent=required; suppression=required; webhooks=required; retries=required; idempotency=required; audit=required       | Planned managed routing decisions may use private intelligence; public route selection remains the interoperable execution boundary.                                                                                                                         |
| `managed-sms-operations`       | Managed SMS operations       | managed-provider-service | planned   | Cloud: planned; Platform API: planned; Self-Hosted: unavailable   | Versioned: `tixkit.messaging-provider/v1` via `@tixkit/domain/messaging#registerMessagingProviderExtension`; implementation: `packages/domain/src/messaging/index.ts#SmsTransport`                                 | credentials=managed-cloud; plaintext-portable=false; portability=configuration-only; rebind=required; consent=required; suppression=required; webhooks=required; retries=required; idempotency=required; audit=required      | Planned Cloud operation may custody credentials and operate delivery; Self-Hosted uses public adapters with operator custody.                                                                                                                                |
| `opencore-email-sdk-adapter`   | OpenCore Email SDK adapter   | public-provider-adapter  | planned   | Cloud: planned; Platform API: planned; Self-Hosted: planned       | Versioned: `tixkit.messaging-provider/v1` via `@tixkit/domain/messaging#registerMessagingProviderExtension`; implementation: `packages/email-transport/src/index.ts#OpenCoreEmailSdkTransport`                     | credentials=caller-or-operator; plaintext-portable=false; portability=configuration-only; rebind=required; consent=required; suppression=required; webhooks=required; retries=required; idempotency=required; audit=required | A public contract shim exists, but provider SDK delivery is not production-ready and remains planned on every adoption path.                                                                                                                                 |
| `plivo-sms-adapter`            | Plivo SMS adapter            | public-provider-adapter  | planned   | Cloud: planned; Platform API: planned; Self-Hosted: planned       | Versioned: `tixkit.messaging-provider/v1` via `@tixkit/domain/messaging#registerMessagingProviderExtension`; implementation: `packages/email-transport/src/index.ts#PlivoSmsTransport`                             | credentials=caller-or-operator; plaintext-portable=false; portability=configuration-only; rebind=required; consent=required; suppression=required; webhooks=required; retries=required; idempotency=required; audit=required | A public outbound adapter exists, but production delivery and reconciliation evidence is incomplete; availability remains planned.                                                                                                                           |
| `rcs-messaging`                | RCS messaging                | managed-provider-service | planned   | Cloud: planned; Platform API: planned; Self-Hosted: unavailable   | Not yet versioned: A public rich-channel extension contract has not been published.                                                                                                                                | credentials=managed-cloud; plaintext-portable=false; portability=contract-metadata; rebind=required; consent=required; suppression=required; webhooks=required; retries=required; idempotency=required; audit=required       | Planned managed rich-channel service. Self-Hosted remains unavailable until a versioned public rich-channel extension contract exists.                                                                                                                       |
| `resend-email-adapter`         | Resend email adapter         | public-provider-adapter  | stable    | Cloud: available; Platform API: available; Self-Hosted: available | Versioned: `tixkit.messaging-provider/v1` via `@tixkit/domain/messaging#registerMessagingProviderExtension`; implementation: `packages/email-transport/src/index.ts#ResendEmailTransport`                          | credentials=caller-or-operator; plaintext-portable=false; portability=configuration-only; rebind=required; consent=required; suppression=required; webhooks=required; retries=required; idempotency=required; audit=required | Implemented public adapter; deployments retain responsibility for provider credentials and account configuration.                                                                                                                                            |
| `smtp-email-adapter`           | SMTP email adapter           | public-provider-adapter  | stable    | Cloud: available; Platform API: available; Self-Hosted: available | Versioned: `tixkit.messaging-provider/v1` via `@tixkit/domain/messaging#registerMessagingProviderExtension`; implementation: `packages/email-transport/src/index.ts#SmtpEmailTransport`                            | credentials=caller-or-operator; plaintext-portable=false; portability=configuration-only; rebind=required; consent=required; suppression=required; webhooks=required; retries=required; idempotency=required; audit=required | Implemented TLS SMTP adapter with normalized failure semantics and deterministic Message-ID reconciliation; deployments retain responsibility for relay credentials, sender authorization and account configuration.                                         |
| `telnyx-sms-adapter`           | Telnyx SMS adapter           | public-provider-adapter  | stable    | Cloud: available; Platform API: available; Self-Hosted: available | Versioned: `tixkit.messaging-provider/v1` via `@tixkit/domain/messaging#registerMessagingProviderExtension`; implementation: `packages/email-transport/src/index.ts#TelnyxSmsTransport`                            | credentials=caller-or-operator; plaintext-portable=false; portability=configuration-only; rebind=required; consent=required; suppression=required; webhooks=required; retries=required; idempotency=required; audit=required | Implemented public adapter; deployments retain responsibility for provider credentials and account configuration.                                                                                                                                            |
| `twilio-sms-adapter`           | Twilio SMS adapter           | public-provider-adapter  | stable    | Cloud: available; Platform API: available; Self-Hosted: available | Versioned: `tixkit.messaging-provider/v1` via `@tixkit/domain/messaging#registerMessagingProviderExtension`; implementation: `packages/email-transport/src/index.ts#TwilioSmsTransport`                            | credentials=caller-or-operator; plaintext-portable=false; portability=configuration-only; rebind=required; consent=required; suppression=required; webhooks=required; retries=required; idempotency=required; audit=required | Implemented public adapter with signed status callbacks, replay-safe delivery reconciliation, and explicit opt-out consent handling; deployments retain responsibility for provider credentials, exact callback URL registration, and account configuration. |
| `vonage-sms-adapter`           | Vonage SMS adapter           | public-provider-adapter  | planned   | Cloud: planned; Platform API: planned; Self-Hosted: planned       | Versioned: `tixkit.messaging-provider/v1` via `@tixkit/domain/messaging#registerMessagingProviderExtension`; implementation: `packages/email-transport/src/index.ts#VonageSmsTransport`                            | credentials=caller-or-operator; plaintext-portable=false; portability=configuration-only; rebind=required; consent=required; suppression=required; webhooks=required; retries=required; idempotency=required; audit=required | A public outbound adapter exists, but production delivery and reconciliation evidence is incomplete; availability remains planned.                                                                                                                           |
| `whatsapp-messaging`           | WhatsApp messaging           | managed-provider-service | planned   | Cloud: planned; Platform API: planned; Self-Hosted: unavailable   | Not yet versioned: A public rich-channel extension contract has not been published.                                                                                                                                | credentials=managed-cloud; plaintext-portable=false; portability=contract-metadata; rebind=required; consent=required; suppression=required; webhooks=required; retries=required; idempotency=required; audit=required       | Planned managed rich-channel service. Self-Hosted remains unavailable until a versioned public rich-channel extension contract exists.                                                                                                                       |

## Contract rules

- Shared behavior and provider adapters cross the repository boundary only through the named public symbols above. Cloud consumes immutable public artifacts and does not copy or patch shared source.
- Credentials, signing keys, and provider tokens are never included in portable bundles. Configuration and contract metadata require destination rebinding where shown.
- Consent, suppression, webhook verification, bounded retries, idempotency, and audit requirements are mandatory wherever marked required; a managed service does not weaken them.
- Apple Messages for Business, RCS, and WhatsApp are planned managed services. They are unavailable to Self-Hosted deployments until Tixkit publishes a versioned rich-channel extension contract.
- The schema permits a pending managed contract to enter private beta on Cloud or Platform API without implying a public extension contract. Every current rich-channel decision remains pinned to planned until this registry records an accepted promotion.
- The optional exact request-ID incident sink is wired only through Tixkit's built-in direct-HTTP messaging adapters. `tixkit.messaging-provider/v1` custom extension factories do not receive this privileged callback. Extension operators must keep provider diagnostics hash-only and must not return exact IDs in transport results, errors, logs, traces, or Temporal payloads; a future versioned contract is required before custom extensions can participate in encrypted exact-ID capture.
- Managed routing intelligence may remain private, but execution must use the public provider-routing boundary and remain auditable.
