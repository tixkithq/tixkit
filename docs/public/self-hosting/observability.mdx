---
title: Configure observability
description: Monitor request, workflow, datastore, provider, export, checkout, and documentation health without logging sensitive data.
audience:
  - self-hoster
product_area: self-hosting
content_type: how-to
status: beta
owner: platform-operations
last_verified: 2026-07-16
prerequisites:
  - A telemetry backend compatible with the repository configuration
related:
  - /operations/incidents
  - /reference/performance
---

Collect service-level metrics, traces, structured logs, and health signals for the API, worker, admin, checkout, docs, database, Redis, Temporal, storage, and configured providers.

## Correlate safely

Use request, workflow, delivery, export-job, checkout-session, and order identifiers only where policy permits. Redact authorization headers, cookies, API keys, signing secrets, provider credentials, ticket codes, and personal-data payloads.

For an outbound provider incident, correlate the Tixkit request or workflow with dependency, operation, outcome, HTTP status, duration, retry classification, and a provider request ID when one is safely available. Do not use raw URLs containing identifiers as metric labels. Do not export authorization headers, client secrets, webhook signatures, full request/response bodies, or customer payloads. A bounded redacted body preview is diagnostic evidence, not application data, and must follow the same access and retention controls as logs.

## Separate dependency failures

Distinguish transport failures and deadlines from provider rate limits, selected `5xx` failures, malformed gateway responses, permanent `4xx` validation/authentication failures, and customer declines. Alert and SLO calculations should identify the dependency and operation without treating a provider rejection as a Tixkit platform crash. Honor provider retry guidance, but investigate retry multiplication when both Temporal and an adapter appear to retry the same operation.

## Alert on outcomes

Alert on error/latency saturation, worker/task-queue lag, failed checkout finalization, payment reconciliation, webhook dead letters, export failures, database pool pressure, storage errors, and documentation smoke failures. Avoid alerts based only on process uptime.

Link alerts to the exact [incident runbook](/operations/incidents) and verify alert routing during release rehearsal.

## Production Helm contract

The Production values require observability to remain enabled. Configure `OTEL_EXPORTER_OTLP_ENDPOINT` in the runtime Secret with an HTTPS collector origin; Tixkit appends `/v1/traces`. If the collector requires headers, set `OTEL_EXPORTER_OTLP_HEADERS` as comma-separated, URL-encoded `key=value` entries. Header values are credentials: keep them in the Secret, never in `OTEL_RESOURCE_ATTRIBUTES` or committed values.

The chart fixes the supported transport to `http/protobuf`, uses parent-based ratio sampling, and labels telemetry with the `tixkit` service namespace and deployment profile. Change the sampling ratio only after checking collector capacity and trace cost.

When `observability.metrics.mode=service-monitor`, the cluster must already have the Prometheus Operator CRDs. The chart creates:

- an API `ServiceMonitor` for `/metrics` using `METRICS_BEARER_TOKEN` from the runtime Secret;
- alert rules for sustained API 5xx rate, API p95 latency, buyer-surface RUM p75 budgets with minimum sample guards, and stalled durable migration progress;
- worker metric delivery through `PROMETHEUS_PUSHGATEWAY_URL`.

The buyer RUM endpoint accepts only the versioned LCP, INP and CLS contract for `checkout` or `event-page`. It rejects unknown and identifying application-payload fields, oversized bodies, non-finite or out-of-range values, and excess requests. It does not persist samples or use them as customer analytics. Browser delivery omits credentials and never retries. The three histogram families contain only the `service` and bounded `surface` labels. Default alerts use a 15-minute p75 after at least 100 samples: 2.5 seconds for LCP, 0.2 seconds for INP and 0.1 for CLS. Proxies and application servers can still observe transport metadata; configure access-log redaction and retention accordingly.

Public RUM submissions are untrusted. Treat them as a diagnostic signal, validate volume and abuse behavior, and do not claim abuse resistance, an SLO or representative user experience from a rendered rule or a small sample. Complete the applicable privacy and retention review before enabling production collection; the minimized application payload does not remove transport-layer privacy obligations.

Workers publish under a per-pod Pushgateway grouping, delete that grouping on graceful shutdown, and bound migration-stall alerts by the grouping's `push_time_seconds`. This prevents a hard-lost pod's last gauge from alerting forever; monitor Pushgateway storage separately and retain its own stale-series cleanup policy.

If another metrics system owns discovery, set the mode to `external` and reproduce the same authenticated scrape and alert semantics outside the chart. Never expose `/metrics` through public ingress.

Before accepting a release, prove a trace reaches the collector, an authenticated scrape succeeds while an unauthenticated scrape returns `401`, worker activity metrics arrive, and a test alert reaches the intended route. A rendered `ServiceMonitor` alone is not runtime proof.
